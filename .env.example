# Conversion tracking — set these in Vercel project settings (Production).
# Both are optional: if unset, the matching script simply does not load.

# Meta Pixel ID (e.g. 123456789012345)
NEXT_PUBLIC_FB_PIXEL_ID=

# GA4 Measurement ID (e.g. G-XXXXXXXXXX)
NEXT_PUBLIC_GA4_ID=
